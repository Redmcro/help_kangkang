# 项目规则（所有 AI 自动生效）

> ⛔ 本文件是全局规则，任何在此项目工作的 AI 都必须遵守。

## 铁律

1. **只改自己模块列出的文件** — `design/TODO.md` 中每个模块有"负责文件"和"不要碰"清单，违者打回
2. **不要开端口** — 不要启动 http-server、live-server、vite 或任何端口服务
3. **不要开浏览器** — 不要用 browser 工具打开页面
4. **不要运行游戏** — 调试和验收由大内总管统一执行（唯一调试端口 9090）
5. **做完就停** — 改完代码报告改动内容，等待大内总管验收。不要自行验证

## 工作方式

- 你的任务在 `design/TODO.md` 中，找到你被指派的模块，按要求完成
- 每个模块包含：任务清单、必读上下文、验收标准
- 完成后向大内总管汇报你的改动，**不要直接向皇上汇报**
